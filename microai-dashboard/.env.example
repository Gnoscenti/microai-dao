
# ---------- MicroAI Dashboard Environment ----------
# Public (exposed at build) variables must be prefixed with VITE_
# Create a copy of this file named `.env.local` and fill in your values.

# === Chain & Program ===
VITE_RPC_URL=https://api.mainnet-beta.solana.com
VITE_PROGRAM_ID=ReplaceWithYourProgramIdBase58
VITE_TREASURY_PUBKEY=ReplaceWithYourTreasuryPubkeyBase58

# === Off-chain indexers / APIs (optional but recommended) ===
VITE_GOVERNANCE_API=https://your-governance-api.example.com
VITE_INDEXER_URL=https://your-indexer.example.com

# === Policy / UX ===
VITE_TIMELOCK_SECONDS=86400
